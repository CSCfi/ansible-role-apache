---
# handlers file for apache
- name: restart apache
  #service:
  #  name: "{{ apache_service_name }}"
  #  state: restarted
  shell: cat /etc/apache2/apache2.conf || systemctl restart apache2 || systemctl status apache2

- name: reown privatekeys-apache
  include_tasks: tasks/apache_certsownership.yml
